
@{
    ViewBag.Title = "FormAlumno";
    Layout = null;
}
@using School.Client.Clases.Controllers;
@model DataAlumno
@*@using (Ajax.BeginForm("CrearProfesor", "School", null, new AjaxOptions { HttpMethod = "POST", OnFailure = "CreacionFallo", OnSuccess = "RespuestaForm", OnBegin = "Begin", OnComplete = "Complete", UpdateTargetId = "ContainerForm" }, new { id = $"FormAlumno", @class = "row", target = "_self" }))*@
@* { *@
<style>
    .TitleModal {
        text-align: center;
        color: var(--colorBase);
        text-shadow: 1px 1px 1px rgba(0,0,0,0.2);
    }

    .ContainerForm {
        border: 1px solid rgba(0,0,0,0.1);
        border-radius: 5px;
    }

    #FormAlumno label {
        color: rgba(0,0,0,0.7);
        font-size: .8rem;
    }

    #FormAlumno input:not(.form-check-input) {
        border: none;
        font-size: .8rem;
        border-bottom: 1px solid rgba(0,0,0,0.2);
        border-radius: 0px;
    }

    #FormAlumno select {
        border: none;
        font-size: .8rem;
        border-bottom: 1px solid rgba(0,0,0,0.2);
        border-radius: 0px;
    }

    .btnModalSave {
        background: var(--colorBase);
        color: white;
        font-size: .85rem;
        margin-right: 1rem;
    }

        .btnModalSave:hover {
            filter: saturate(170%);
            color: white;
        }

    .btnModalCancel {
        background: rgba(0,0,0,0.2);
        font-size: .85rem;
        color: white;
    }

        .btnModalCancel:hover {
            filter: contrast(150%);
            color: white;
        }

    .TitleSection {
        color: var(--colorBase);
        font-weight: 500;
        font-size: .9rem;
    }

    input[type=number]::-webkit-inner-spin-button,
    input[type=number]::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    .btnModal {
        transition: ease .3s all;
        width: 17%;
    }

        .btnModal:hover {
            width: 20%;
        }

            .btnModal:hover .IconsButtons {
                width: 10%;
                opacity: 1;
            }

    .IconsButtons {
        padding-right: 2%;
        width: 0px;
        opacity: 0;
        transition: ease .3s all;
    }
</style>
@using (Ajax.BeginForm("CrearAlumno", "School", null, new AjaxOptions { HttpMethod = "POST", OnFailure = "CreacionFallo", OnSuccess = "RespuestaForm", OnBegin = "Begin", OnComplete = "Complete", UpdateTargetId = "ContainerForm" }, new { id = $"FormAlumno", @class = "row", target = "_self" }))
{
    @Html.Hidden("AlumnoID", Model.AlumnoID)
    <div class="col-md-4">
        <label for="inputNombre" class="form-label">Nombre</label>
        <input name="Nombre" type="text" class="form-control w-70" id="inputNombre" value="@Model.Nombre" required>
    </div>
    <div class="col-md-4">
        <label for="inputApellido" class="form-label">Apellido</label>
        <input name="Apellido" type="text" class="form-control w-70" id="inputApellido" value="@Model.Apellido" required>
    </div>
    <div class="col-md-4">
        <label for="inputIdentificacion" class="form-label">Identificación</label>
        <input name="Identificacion" type="number" class="form-control w-50" value="@Model.Identificacion" min="9999999" max="99999999999" id="inputIdentificacion" required>
    </div>
    <div class="col-md-4">
        <label for="inputDireccion" class="form-label">Direccion</label>
        <input name="Direccion" type="text" class="form-control" id="inputDireccion" value="@Model.Direccion" required>
    </div>
    <div class="col-md-4">
        <label for="inputTelefono" class="form-label ">Telefono</label>
        <input name="Telefono" type="text" class="form-control w-60" id="inputTelefono" value="@Model.Telefono" required>
    </div>
    <div class="col-md-2">
        <label for="inputEdad" class="form-label">Edad</label>
        <input name="Edad" type="tel" class="form-control w-30" id="inputEdad" max="100" value="@Model.Edad" required>
    </div>
    <div class="col-md-2">
        <label for="inputEdad" class="form-label">Activo</label>
        <div class="form-check form-switch">
            <input name="Activo" value="true" class="form-check-input" type="checkbox" id="flexSwitchCheckChecked" value="@Model.Activo" @(Model.Activo == true ? "checked" : "")>
        </div>
    </div>
    <div class="col-sm-12 mt-3 p-0 d-flex justify-content-end">
        <button class="btn btnModal btnModalSave" type="submit" >
            <i class="IconsButtons fa-solid fa-floppy-disk"></i>
            Guardar
        </button>
        <button class="btn btnModal btnModalCancel" type="button" data-bs-dismiss="modal">
            <i class="IconsButtons fa-solid fa-ban"></i>
            Cancelar
        </button>
    </div>
}

